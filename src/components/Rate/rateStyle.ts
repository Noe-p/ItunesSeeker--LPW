import { StyleSheet } from 'react-native';

export const rateStyle = StyleSheet.create({
  detailSongRate: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    width: '95%',
    marginTop: 10,
  },
});
